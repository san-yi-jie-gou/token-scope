name: Build App Store Archive

on:
  workflow_dispatch:

jobs:
  archive:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Select stable Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate

      - name: Run tests
        run: |
          xcodebuild \
            -project TokenScope.xcodeproj \
            -scheme TokenScope \
            -configuration Debug \
            -destination 'platform=macOS' \
            -derivedDataPath build-ci \
            CODE_SIGNING_ALLOWED=NO \
            test

      - name: Create adhoc-signed archive
        run: |
          xcodebuild \
            -project TokenScope.xcodeproj \
            -scheme TokenScope \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -archivePath "$RUNNER_TEMP/TokenScope-1.0.0-12.xcarchive" \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            archive

      - name: Record build environment
        run: |
          sw_vers
          xcodebuild -version
          /usr/libexec/PlistBuddy -c 'Print :ApplicationProperties:CFBundleVersion' "$RUNNER_TEMP/TokenScope-1.0.0-12.xcarchive/Info.plist"
          /usr/libexec/PlistBuddy -c 'Print :BuildMachineOSBuild' "$RUNNER_TEMP/TokenScope-1.0.0-12.xcarchive/Products/Applications/TokenScope.app/Contents/Info.plist"
          codesign -d --entitlements :- "$RUNNER_TEMP/TokenScope-1.0.0-12.xcarchive/Products/Applications/TokenScope.app"
          codesign -d --entitlements :- "$RUNNER_TEMP/TokenScope-1.0.0-12.xcarchive/Products/Applications/TokenScope.app/Contents/PlugIns/TokenScopeWidget.appex"

      - name: Package archive
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "$RUNNER_TEMP/TokenScope-1.0.0-12.xcarchive" \
            "$RUNNER_TEMP/TokenScope-1.0.0-12-xcarchive.zip"

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: TokenScope-1.0.0-12-xcarchive
          path: ${{ runner.temp }}/TokenScope-1.0.0-12-xcarchive.zip
          retention-days: 3
          if-no-files-found: error
